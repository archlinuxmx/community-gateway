---
version: 2.1
jobs:
  first:
    docker:
      - image: archlinux
    steps:
      - checkout
      - run: |
          mkdir -p /var/lib/pacman/
          pacman -Sy
          pacman -S ansible-lint --noconfirm
workflows:
  version: 2.1
  community-gateway:
    jobs:
      - first

---
version: 2.1
jobs:
  first:
    docker:
      - image: archlinux
    steps:
      - checkout
      - run: pacman -S ansible-lint --no-confirm
workflows:
  version: 2.1
  community-gateway:
    jobs:
      - first
